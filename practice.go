package main

import (
	"net/http"
)

func raceProviders(providers []Provider, body []byte) *http.Response {
	// TODO: finish this — launch a goroutine per provider, return the first response
	return nil
}